# AI Agent Instructions

Follow the repository-wide rules in `AGENTS.md`.

For feature, backlog, rollout, remediation, or vague continuation work:

1. Read `BACKLOG.md` to select one workstream.
2. Read `backlogs/AGENTS.md`, `backlogs/protocol.md`, and only the selected active backlog.
3. Do not load completed archives or `reviews/tasks/**` unless reconciliation, regression investigation, or the selected item requires them.
